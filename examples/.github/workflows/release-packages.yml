name: Example release-packages (deprecated)

on:
  push:
    tags:
      - "v*"
      - "V*"

jobs:
  noop:
    runs-on: ubuntu-latest
    if: false
    steps:
      - name: Deprecated
        run: echo "This example workflow is deprecated or for another project. Remove or adapt to this repo as needed."
