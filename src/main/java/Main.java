/**
 * Placeholder main class retained for compatibility with older launchers.
 *
 * The real application entry point is `com.studentgui.bootstrap.Bootstrap`.
 */
final class PlaceholderMain {
    private PlaceholderMain() { throw new AssertionError("no instances"); }
}
